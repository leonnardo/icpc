#include <cstdio>

int main(int argc, char const *argv[])
{
    int m, n, caso = 1, rodadas;
    int vitorias[20];
    scanf ("%d %d", &m, &n);
    while (m != 0 || n != 0) {
        for (int i = 0; i < 20; i++)
            vitorias[i] = 0;
        rodadas = 0;

        printf("Case %d: %d\n", caso, res);
        caso++;
        scanf("%d %d", &m, &n);
    }
    return 0;
}